name: Patch Showdown No Timeout

on:
  push:
    paths:
      - '.github/workflows/patch-showdown-no-timeout.yml'
      - '.github/scripts/patch_showdown_no_timeout.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Remove Showdown solver timeout
        run: python .github/scripts/patch_showdown_no_timeout.py
      - name: Syntax check
        run: python -m py_compile streamlit_app.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet; then exit 0; fi
          git add streamlit_app.py
          git commit -m "Remove Showdown solver time limit"
          git push
